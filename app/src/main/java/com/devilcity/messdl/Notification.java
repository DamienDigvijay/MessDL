package com.devilcity.messdl;

/**
 * Created by Digvijay on 10/27/2015.
 */
public class Notification extends android.app.Notification {
}

/*if(R.id.notification == item.getItemId()) {
        Notification a = new Notification();
        if(a.Index == 1){
        a.Index=0;
        Notu="OFF";
        }
        if(a.Index == 0){
        a.Index = 1;
        Notu  = "ON";
        }

        AlertDialog.Builder builder = new AlertDialog.Builder(this);
        builder
        .setMessage("Your Notification is currently turned "+Notu)
        .setIcon(R.drawable.noti)
        .setTitle("Current Status")
        .setCancelable(false)
        .setNeutralButton("OK", new DialogInterface.OnClickListener() {
@Override
public void onClick(DialogInterface dialog, int which) {
        }
        })
        .create();
        AlertDialog alert = builder.create();
        alert.show();

        }*/